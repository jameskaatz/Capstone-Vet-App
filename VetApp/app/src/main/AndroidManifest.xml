<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vetapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name =".globalClass"
        tools:ignore="ExtraText">
        <activity android:name=".MoreSymp"></activity>
        <activity android:name=".NewAnimals" />
        <activity android:name=".CasesActivity" />
        <activity android:name=".ConversationActivity" />
        <activity android:name=".NewConversationActivity" />
        <activity android:name=".UserSearchActivity" />
        <activity android:name=".AddContactActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewAnimals"
            android:label="@string/title_activity_view_animals"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".VetView" />
        <activity android:name=".FarmView" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>